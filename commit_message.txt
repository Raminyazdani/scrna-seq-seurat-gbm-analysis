Ramin Yazdani | scrna-seq-seurat-gbm-analysis | main | WIP(data): Parse expression matrix

Added expression matrix parsing:
- Read TPM expression matrix from downloaded file
- Set genes as row names, cells as columns

BUG INTRODUCED: Not checking if file exists before attempting to read. This will cause cryptic error on first run or if data/ directory is missing.

Status: BROKEN - Will require hotfix in next step.
